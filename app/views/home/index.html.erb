<div class="container-fluid">
  <div class="row">
    <!-- ░░░ Economy ░░░ -->
    <div class="col-md-4 mb-4">
      <%= turbo_frame_tag "economy-panel" do %>
        <%= render partial: "shared/economy", locals: { economy: @economy } %>
      <% end %>
    </div>

    <!-- ░░░ Mayhem ░░░ -->
    <div class="col-md-4 mb-4">
      <div class="card">
        <div class="card-header">⚡ Latest Mayhem</div>
        <%= turbo_frame_tag "mayhem-list", class: "scroll-panel d-block" do %>
          <ul class="list-group list-group-flush">
            <%= render partial: "home/mayhem_row",
                       collection: @recent_transactions,
                       as: :transaction %>
          </ul>
        <% end %>
      </div>
    </div>

    <!-- ░░░ Complaints ░░░ -->
    <div class="col-md-4 mb-4">
      <div class="card">
        <div class="card-header">🤔 Who’s Complaining?</div>
        <%= turbo_frame_tag "complaints-list", class: "scroll-panel d-block" do %>
          <ul class="list-group list-group-flush">
            <%= render partial: "home/complaint_row",
                       collection: @recent_customers,
                       as: :customer %>
          </ul>
        <% end %>
      </div>
    </div>
  </div>

  <!-- ░░░ Juices Table ░░░ -->
  <div class="row">
    <div class="col-12">
      <div id="juice-panel">
        <%= render partial: "home/juices", locals: { juices: @juices } %>
      </div>
    </div>
  </div>
</div>
