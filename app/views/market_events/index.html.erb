<h1 class="mb-3">📈 Market Events</h1>

<table class="table table-sm table-striped">
  <thead class="table-dark">
    <tr>
      <th>Time</th>
      <th>Event</th>
      <th class="text-end">Chaos Δ</th>
      <th class="text-end">Inflation Δ</th>
    </tr>
  </thead>
  <tbody>
    <% @events.each do |ev| %>
      <tr>
        <td><%= ev.time_label %></td>
        <td><%= ev.description %></td>
        <td class="text-end"><%= ev.chaos_delta %></td>
        <td class="text-end">
          <%= number_to_percentage(ev.inflation_delta * 100, precision: 2) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
